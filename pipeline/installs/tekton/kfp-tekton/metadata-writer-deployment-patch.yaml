apiVersion: apps/v1
kind: Deployment
metadata:
  name: metadata-writer
spec:
  template:
    spec:
      containers:
      - name: main
        env:
        - name: ENABLE_LOGGING
          valueFrom:
            configMapKeyRef:
              name: kfp-tekton-config
              key: enable_logging
